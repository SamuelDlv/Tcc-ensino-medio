# Área pública

Esta pasta é o document root da aplicação. Inicie o desenvolvimento com `php -S 127.0.0.1:8000 -t public` e acesse `/index.php`.

Somente arquivos destinados ao navegador devem ficar publicados aqui. A configuração do banco e as funções internas ficam em `../config`.
